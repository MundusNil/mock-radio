{
  "name": "mock-radio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AI 氛围电台：音乐永远是主体，梦可按自己的节奏轻轻串场。",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/start.mjs",
    "stop": "node scripts/stop.mjs",
    "setup:voice": "python -m pip install edge-tts",
    "setup:ffmpeg": "node scripts/setup-ffmpeg.mjs",
    "dev:station": "pnpm --filter @mock-radio/station dev",
    "dev:web": "pnpm --filter @mock-radio/web dev",
    "scan": "pnpm --filter @mock-radio/station scan",
    "voice:compare": "pnpm --filter @mock-radio/station voice:compare",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.9",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
